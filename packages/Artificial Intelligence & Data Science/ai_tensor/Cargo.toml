[package]
name = "ai_tensor_lib"
version = "0.1.0"
edition = "2024"
authors = ["OmniSyntax"]
license = "MIT OR Apache-2.0"

[dependencies]
# Add any math or utility dependencies your AI package needs here
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "ai_tensor_lib"
path = "src/lib.rs"